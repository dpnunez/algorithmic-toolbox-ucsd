while(current_number >= 1) {
	// 	// -1
	// 	int aux_number = current_number - 1;
	// 	int min_op = amount_operations_table[aux_number - 1];

	// 	if(current_number % 2 == 0) {
	// 		if(amount_operations_table[(current_number - 1) / 2] < min_op) {
	// 			aux_number = 
	// 		}
	// 	} 
		

	// 	// /2
	// 	if((current_number_index + 1) % 2 == 0 && amount_operations_table[current_number_index / 2] < min_op) {
	// 		min_op = amount_operations_table[current_number_index / 2];
	// 		aux_number = current_number_index / 2;
	// 	}
	// 	if((current_number_index + 1) % 2 == 0 && amount_operations_table[current_number_index / 3] < min_op) {
	// 		min_op = amount_operations_table[current_number_index / 3];
	// 		aux_number = current_number_index / 3;
	// 	}

	// cout << "from: " << (current_number_index + 1) << " to: " << (aux_number + 1) << std::endl;
	// 	sequence.push_back(aux_number + 1);
	// 	current_number_index = aux_number;
	// }